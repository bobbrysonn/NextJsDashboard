import { Karla } from 'next/font/google';
import { Lusitana } from 'next/font/google';

const karla = Karla({ subsets: ['latin'] });
const lusitana = Lusitana({ weight: ['400', '700'], subsets: ['latin'] });

export { karla, lusitana };
